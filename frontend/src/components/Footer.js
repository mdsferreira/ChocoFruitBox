import React from 'react';
// import { Button, Card, CardImg, CardBody, CardTitle, CardSubtitle, CardText } from 'reactstrap';

const Footer = (props) => {
    return (
        <section id="footer">teste
        </section>
    ); 
}

export default Footer;